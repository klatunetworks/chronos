name: run tests
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        java:
          - '8'
          - '11'
        java-distribution:
          - temurin
          - oracle
        cassandra:
          - '5'
        redis:
          - '8'

    services:
      cassandra:
        image: cassandra:${{ matrix.cassandra }}
        ports:
          - 9042:9042
          - 7000:7000
          - 7001:7001
          - 7199:7199
        env:
          CASSANDRA_CLUSTER_NAME: TestCluster
          MAX_HEAP_SIZE: 1G
          HEAP_NEWSIZE: 256M
        options: >-
          --memory 2G
          --memory-reservation 1G
          --health-cmd "cqlsh -e 'describe keyspaces'"
          --health-interval 30s
          --health-retries 10
          --health-timeout 10s

      redis:
        image: redis:${{ matrix.redis }}
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-retries 5
          --health-timeout 3s

    name: Java ${{ matrix.java-distribution }}/${{ matrix.java }} sample
    steps:
      - uses: actions/checkout@v7

      - name: Setup java
        uses: actions/setup-java@v5
        with:
          distribution: ${{ matrix.java-distribution }}
          java-version: ${{ matrix.java }}

      - shell: bash
        run: ./gradlew clean test
